name: Publish Javadoc

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  javadoc:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Generate Javadoc
        run: mvn javadoc:javadoc -DskipTests -q

      - name: Prepare pages structure
        run: |
          mkdir -p _site/apidocs
          if [ -d target/site/apidocs ]; then
            cp -r target/site/apidocs/* _site/apidocs/
          elif [ -d target/apidocs ]; then
            cp -r target/apidocs/* _site/apidocs/
          elif [ -d target/reports/apidocs ]; then
            cp -r target/reports/apidocs/* _site/apidocs/
          else
            echo "Javadoc output not found" && find target -name "*.html" | head -5
            exit 1
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
